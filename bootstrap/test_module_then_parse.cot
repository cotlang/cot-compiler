// Test creating module then checking parser
import "../src/token"
import "../src/lexer"
import "../src/ast"
import "../src/parser"

fn main() i64 {
    println("Creating parser...")

    const source = "fn main() { }"
    var p = newParser(source)
    println("Parser created")

    println("Creating module...")
    var mod = newModule()
    println("Module created")

    println("Calling parserIsAtEnd after module creation...")
    const at_end = parserIsAtEnd(&p)
    println("At end: " + string(at_end as i64))

    println("Done!")
    return 0
}
